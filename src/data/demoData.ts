/**
 * Demo / Fallback Data
 *
 * This module exports mock data for use as placeholder/fallback
 * when the backend API is unavailable. In production with a live backend,
 * this data is only shown momentarily while the real data loads.
 */

export {
  deals,
  marketSignals,
  aiInsights,
} from './mockData';
