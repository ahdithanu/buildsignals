name: e2e

# Browser-level tests boot an isolated API/database and frontend on relevant PRs.
on:
  workflow_dispatch:
  pull_request:
    paths:
      - "src/**"
      - "app/**"
      - "e2e/**"
      - "package.json"
      - "package-lock.json"
      - "requirements.txt"
      - ".github/workflows/e2e.yml"

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      # The Playwright config's webServer boots the backend with these; set
      # here too so anything the workflow runs directly sees consistent values.
      DATABASE_URL: "sqlite:///./e2e-test.db"
      SECRET_KEY: "e2e-ci-secret-key-not-for-production"
      ENVIRONMENT: "ci"
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Node deps
        run: npm ci --no-audit --no-fund

      - name: Install Playwright browser
        run: npm run e2e:install

      - name: Run E2E
        # The config's `webServer` boots both the FastAPI backend (:8000,
        # migrated throwaway DB, rate limits raised) and the Vite dev server
        # (:8080). No manual server startup needed here.
        run: npm run e2e

      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            playwright-report/
            test-results/
          retention-days: 14
