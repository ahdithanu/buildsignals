name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: "sqlite:///./ci.db"
      SECRET_KEY: "ci-test-secret-key-not-for-production"
      ENVIRONMENT: "ci"
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest httpx pip-audit

      # Fails the build on any known-vulnerable dependency. Uses the PyPI
      # advisory DB (osv.dev). To skip a CVE we've reviewed and confirmed
      # doesn't affect us, add --ignore-vuln=<ID> with a comment.
      #
      # Currently-ignored, with the fix path documented so this list
      # shrinks rather than grows:
      #
      # pyasn1 CVE-2026-30922 — pinned transitively by python-jose
      #   (pyasn1<0.5.0). Fix: wait for a python-jose release that loosens
      #   the bound, or migrate to PyJWT (worth evaluating regardless).
      #
      # (The starlette CVE cluster was cleared by bumping fastapi 0.111→0.139
      #  which brings starlette>=1.3.1 — no longer ignored.)
      - name: pip-audit — CVE scan
        run: |
          pip-audit --strict --requirement requirements.txt \
            --ignore-vuln CVE-2026-30922

      - name: Run Alembic migrations
        run: alembic upgrade head

      - name: Alembic drift check (no un-migrated model changes)
        run: |
          set -e
          # Try `alembic check` (Alembic >= 1.9). If unavailable or it errors out
          # with the wrong signature, fall back to autogenerate + diff inspection.
          if alembic check 2>&1 | tee /tmp/alembic_check.log; then
            echo "alembic check passed"
          else
            echo "alembic check failed or unavailable; falling back to autogenerate diff"
            alembic revision --autogenerate -m ci_drift_check 2>&1 | tee /tmp/alembic_autogen.log
            GENERATED=$(ls -t alembic/versions/*ci_drift_check*.py | head -n1)
            if [ -z "$GENERATED" ]; then
              echo "No autogenerate file produced — treating as no drift."
              exit 0
            fi
            # If the generated migration's upgrade body only contains `pass`, no drift.
            if grep -E "^\s*pass\s*$" "$GENERATED" > /dev/null && \
               ! grep -E "op\.(create|drop|add|alter|rename)" "$GENERATED" > /dev/null; then
              echo "No schema drift detected."
              rm -f "$GENERATED"
              exit 0
            else
              echo "Schema drift detected — model changes lack a migration."
              cat "$GENERATED"
              rm -f "$GENERATED"
              exit 1
            fi
          fi

      - name: Run tests
        run: pytest -q
